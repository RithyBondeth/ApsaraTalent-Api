# The project mixes @nestjs/core@10 with @nestjs/websockets@11, so npm's strict
# peer-dependency resolution (default since npm v7) rejects `npm install`/`npm ci`.
# This repo's lockfile was built with legacy resolution; pin it so installs are
# reproducible locally and in CI.
legacy-peer-deps=true
